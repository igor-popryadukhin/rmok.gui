import Vue from 'vue'

const vueScrollOptions: any = Vue.extend({
  data () {
    return {
      vueScrollOptions: {
        bar: {
          background: '#c912c6'
        }
      }
    }
  }
})

export default vueScrollOptions
