export enum Role {
  R_ADMIN = 'r_admin', // This is the administrator
  R_LEADER_CC = 'r_leader_cc', // This is the head of the call center
  R_LEADER_GROUP = 'r_leader_group', // This is the team leader
  R_OPERATOR = 'r_operator' // This is the operator
}
