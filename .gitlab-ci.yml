stages:
  - build
  - deploy

build dev:
  image: node:14.13.0
  stage: build
  only:
    - tags
  script:
    - npm install -g @vue/cli@latest
    - npm install
    - npm run build
  artifacts:
    paths:
      # Build folder
      - dist/
    expire_in: 1 hour
